# === CLOUDFLARE ===
CLOUDFLARE_DATABASE_NAME="clover-db"

# === JWT (replaces NextAuth) ===
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET="your-random-secret-here"

# === NEXT ===
NEXT_PUBLIC_URL="http://localhost:3000"
